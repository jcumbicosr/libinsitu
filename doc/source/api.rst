Python API
==========

We currently expose only a single python function, responsible for querying local or remote (via OpenDAP) 
NetCDF files and transform them into pandas DataFrame.

.. autofunction:: libinsitu.common.netcdf_to_dataframe

